spring:
  # MySQL 配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/easydata_sample?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: root

mybatis:
  type-aliases-package: io.github.jitwxs.easydata.sample.bean
  mapper-locations: classpath:mapper/*Mapper.xml